<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="TheFramework.ContentViews.Expander"
             x:Name="expander">
  <ContentView.Content>
        <StackLayout BindingContext="{Binding Source={x:Reference expander}}" Orientation="Vertical">
            <StackLayout.GestureRecognizers>
                <TapGestureRecognizer Tapped="OnTapped"/>
            </StackLayout.GestureRecognizers>
            <ContentView x:Name="mainView" Content="{Binding MainContent}"/>
            <ContentView x:Name="expandedView" Content="{Binding ExpandedContent}"/>
      </StackLayout>
      
  </ContentView.Content>
</ContentView>